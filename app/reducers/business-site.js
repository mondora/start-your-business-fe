const defaultState = {
    editMode: null,
    siteConfig: {
        logoId: null,
        mainColorCode: null,
        templateId: null
    }
};

const businessSite = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default businessSite;